version: '3.4'

services:
  integrationtest:
    image: ${DOCKER_REGISTRY-}integrationtest
    build:
      context: .
      dockerfile: IntegrationTest\Dockerfile
    env_file:
        -   ./IntegrationTest/Environments/gatodev.env
    ports:
        - "${ListenPort}:${ListenPort}"
 
